import React from "react";
import { Parallax } from "react-materialize";

import ChangePassForm from "../forms/password";

const Password = () => (
  <div>
    <h4>Reset Password</h4>
    <div>
      <ChangePassForm />
    </div>
  </div>
);

export default Password;
